# POS連携の現在地（2026-09-20 時点）

他のセッション／他アプリ（PC側 Claude Code 等）がそのまま続きを進められるよう、
**確定した事実・未確定の点・次の一手**をここにまとめる。推測と確認済みを混ぜない。

---

## 0. そもそも何をしたいか

月次の店長会資料で、**全店の 売上・客数・原価** が揃っている状態にする。

供給元は3つ。**原価はFWを正とし、FWで取れない分だけ他POSから補う。**

| 出どころ | 対象 | 状態 |
| --- | --- | --- |
| FW（Foodist Journal） | 24店中23店 | 既存パイプラインで稼働中（`amami-cell/hansoku`） |
| 新Uレジ | 1766 ぎふや福岡天神（1店のみ） | **売上・客数・原価とも取得できるようになった** |
| ダイニー | FWで原価が取れない店の補完 | 原価の出口が判明。未実装 |

---

## 1. FWのカバレッジ（2026-09-20 実測）

`config/stores.yaml` の `pos:` が出どころ。24店中：

- `pos: uleji` … **1766 ぎふや福岡天神 の1店だけ**（本人確認済み）
- `pos: dainy` … **0件**。ダイニーのレジを使う店でも実績はFWに流れている

### 原価（ABCの部門グリッド由来）について

原価率は `hansoku/web/export.py` の `bucket.cost_rate` が作る。元はABCの**部門**。
部門が取れない店は原価率が作れない。

**2026-09-20、ひよこ飯店とたいだいは解消を確認した。**

```
fw.yml → abc-store-probe --abc-levels 部門  2026/08/01〜08/25
  1069 ひよこ飯店 … 数えられる部門行 36件 / 視覚行 574 / ラジオclick=True
  1137 たいだい   … 数えられる部門行 34件 / 視覚行 586 / ラジオclick=True
```

かつては4方式すべてで0件だった。FW側で商品に部門が紐付けられたため。

⚠️ **`abc_dept: false` を外さないと、FWが直っても取込が部門を飛ばし続ける。**
→ hansoku の PR #1 でマージ済み。ABC再取込も実行済み。

**ただし過去月は埋まらない。** `abc-store-ingest --month 2025-09..2026-08` の結果、
部門が取れたのは **2026-08 のみ**で、2025-09〜2026-07 の11ヶ月は0件だった
（商品ABCは12ヶ月ぶん入っている。554件 / 550件）。紐付けが最近行われたため
FW側に過去分が無い。**この2店の原価率は 2026-08 以降でのみ作れる。**
なお部門0件の月があると取込は exit 1 で終わるが、**書き込み自体は完了している**。

過去月だけ部門が無い店（1111 ちゃーちゃん / 1151 NagaGutsu / 1168 GOLD京都ポルタ）は
2026-07以降は取れており、それ以前の穴は埋まらないものとして扱う。
**この3店を `abc_dept: false` にしてはいけない**（今後の部門が入らなくなる）。

---

## 2. 新Uレジ（`amami-cell/pos-sync-public`）

### 2-1. できるようになったこと

**ログイン → メール認証コードの自動取得・入力 → 対象月の指定 → CSV取得 まで全自動。**

```
[uleji] ★メール認証コード(OTP)画面を検出: #authenticationCode が表示されている
[uleji] 認証コードを再送させました
[otp] 認証コードを取得（6桁、1回目の確認）
[uleji] 認証コードによるログインに成功しました
[uleji] 店舗セレクトの選択肢 1件:
    | 0009	0009：大衆酒場 ぎふや
[uleji] 2026-08 のCSVを取得: 1113バイト / UTF-8（BOM付き）
[uleji] 2026-08: 1店取得
```

### 2-2. 認証コード（OTP）の仕組み

管理画面に「この端末を記憶する」は**無い**。ログインのたびに必ずメールでコードが来る。
省略できないので、**実行しているプロセス自身がIMAPでメールを読みに行く**。

- 届け先: 取込専用に作った新Uレジの担当者アカウントに紐づく Gmail
- 差出人 `no-reply@pos.usen-regi.com` ／ 件名「【USENレジ】認証コードのお知らせ」
- 本文「認証コード：（6桁）」／**有効期限15分**
- 「認証コード再送」を押してから取りに行く（前回の古いコードを拾わないため）

**差出人での絞り込みが要**。個人のメールボックスにはGoogle等の「認証コード」メールも
届くので、件名の語だけだと別サービスのコードを入れてしまう。
転送で From が書き換わる場合に備え、Return-Path / Sender / Reply-To / 本文も見る。

メールは**読むだけ**。`select(readonly=True)` + `BODY.PEEK[]` で既読にもしない。
削除も移動もしない。取得したコードはログに出さない（桁数だけ）。

`tests/test_otp.py` で6ケース検証済み（`python tests/test_otp.py`）。

### 2-3. 画面の歩き方（ここで何度も転んだ）

- **URL直打ちは禁止。** `/cms/side-menu/*` を goto すると「予期せぬエラー」になり、
  **セッションごと落ちてログイン画面に戻される**。人と同じようにメニューをクリックする。
- **開いているメニューをもう一度押さない。** 親を押すと畳まれて子が隠れる。
  子が見えているなら親は押さない。
- **「分析」は別タブで開く。** 元のタブを見ていると「何も起きていない」ように見える。
  クリック後に増えたタブを掴む。

### 2-4. 売上CSVの出口

**売上管理 ＞ 過去売上実績**（`/cms/slip-list` ではない）

| 要素 | id/name |
| --- | --- |
| 年 | `designatedYear` |
| 月 | `designatedMonth` |
| 店舗 | `storeCode` |
| 入力グリッド | `pastSalesInfoList[N].sales` / `.guests` |

年月を選ぶ → **検索** → **CSVダウンロード**。

落ちるCSV: UTF-8(BOM) / `店舗コード | 日付 | 売上 | 客数` / 日別31行。**原価の列は無い。**

⚠️ **この画面は「売上を登録する」画面**。`登録` `CSV一括設定` `削除` `更新` `取込`
`アップロード` には**絶対に触らない**（`FORBIDDEN` で防御済み）。読み取りしかしない。

### 2-5. 月次への合算で気をつけたこと

**空欄やハイフンの日を0円として足さない。** 0で足すと未入力の日が「売上0円の日」として
混ざり、月次が静かに小さくなる。前年比や達成率が狂う。値がある日だけ足し、
何日分を足したかを備考に残す。

```
備考: 新Uレジ 日別31日分を合算（売上入力31日）／マスタで新Uレジの店が1件のため対応付け
```

### 2-6. 店舗の対応付け

新Uレジ側の表示は「**大衆酒場 ぎふや**」。マスタには **ぎふや天満橋(1739)** と
**ぎふや福岡天神(1766)** があり、**この名前だけでは特定できない**。

そこで「CSVの店舗が1つ **かつ** マスタで `pos: uleji` の店も1つ」のときだけ結ぶ。
実測で店舗セレクトの選択肢は1件（`0009：大衆酒場 ぎふや`）、本人確認も取れている。
どちらかが増えたら推定はやめ、店舗コードは空のまま報告する。

### 2-7. 原価も取れた（2026-09-20 確定）

**分析（別タブ） → FLコストのカードの「詳細を表示」 → 損益(PL)集計 → CSV**

```
https://analytics-pc.usen-regi.com/v2/pl/sheet
```

落ちるCSV: UTF-8(BOM) / **53列**。原価に関係する列：

```
材料原価 | 総原価 | 粗利益 | 材料原価率 | F/Lコスト | F/L率 | 人件費率
売上予算 | 売上実績 | 総人件費予算 | 総人件費実績 | 総コスト | 営業利益
水道代 | 電気代 | ガス代 | 雑費 | 販管費(インフォマート)
来店者数 | 客単価 | 伝票枚数 | ドリンク / 売上 | フード / 売上 | サービス / 売上
```

**ハマりどころが3つある。**

1. **ダッシュボードはカードが縦に並び、カードごとに「詳細を表示」がある。**
   見えている先頭を押すと売上のカードに行き、`/v2/sales/analysis` に着いて
   「原価が無い」と誤判定する。FLコストのカードの中のボタンを名指しで押す
   （`_open_card()`）。
2. **SSOの転送を待つ。** 分析サイトは `/sso-auth` を経由してから本画面に変わる。
   待たずに読むと中身が空で、やはり「原価が無い」と誤判定する（`_wait_sso()`）。
3. **このSSO URLのクエリには認証トークンが載る。** 公開リポジトリのログに出さない。
   URLを出すときは `C.safe_url()` でクエリを落とす（2026-09-20 に一度出してしまい、
   該当runのログを削除した）。

⚠️ **`原価・販管費登録` と `予算登録` は書き込み画面。触らない。**

#### 残っているのは「月の指定」だけ（未実装）

損益PL集計のCSVは **画面に出ている期間ぶんしか落ちない**。つまり月を指定できないと
当月しか取れず、過去分の埋め戻しができない。ここがまだ出来ていない。

過去売上実績の側は `#designatedYear` / `#designatedMonth` という素の `<select>` で、
`_select_month()` がそのまま効く。**分析サイトは別実装で、前回の棚卸しでは
`input` も `select` も0件**だった（`diag/t_FLコスト_fields.tsv` が見出しだけ）。
日付ピッカーが `div` で組まれていると見るのが自然。

セレクタを当てずっぽうで書くと「押せたつもりで当月のまま」になり、
**どの月を出しても同じ数字が入る**という最悪の壊れ方をする。
そこで先に正体を出す `_period_probe()` を足した。PL集計の画面に着いた時点で

- `input / select / textarea`（value は出さない。`select` は選択肢の**ラベル**まで）
- `[class*=picker] [class*=date] [class*=calendar] [class*=month] [class*=period] [role=combobox]`
- 画面に出ている「2026/09」「2026年9月」風の表示（＝いま何月を見ているか）

を診断ログに出す。**年月そのものは秘密ではないので伏せない**（`mask_numbers` を
かけると何も分からなくなる）。伏せるのは入力済み `value` だけ。

次の一手: `POS_DIAG=1` で `pos_sync.yml` を1回流し、まとめの
「期間の部品しらべ」を見てから `_select_pl_month()` を書く。

**未解決**: このCSVは**1行だけ**（画面の日付範囲ぶんの集計）。月次で取るには
画面の期間指定を操作する必要がある。また**フード原価／ドリンク原価の分解は無い**
（材料原価は総額）。「ドリンク / 売上」「フード / 売上」の構成比はあるので按分はできるが、
それは推定値であって実原価ではない。出力スキーマに入れるなら備考にその旨を残すこと。

---

## 3. ダイニー（`amami-cell/pos-sync-public`）

### 3-1. できていること

ログイン → 92店舗の全選択 → 期間指定 → ZIP取得 → CSV解析 まで通っている。

### 3-2. 原価の出口（サポート回答 2026-09、**未実装**）

今まで見ていた「**データ出力・連携**」(`/aggregatedData/daily/export`) は**場所違い**だった。
15帳票に原価が無かったのは当然。正しくは：

> **ダッシュボード ＞ 経営管理タブ ＞ CSVダウンロード**
> - **原価集計（日別）** … ※実原価に関するデータは**ダイニー経営管理契約店舗のみ**
> - **出数集計** … 販売数。メニュー原価と掛けて商品別の総原価を算出できる
>
> https://dinii.wraptas.site/5d140f1f09354f109133e04a246594df
>
> メニューごとの原価はメニューマスターのCSV一括編集でも確認・編集できる
> https://dinii.wraptas.site/2f671045ad74801d8c3beb1d14172bcc

⚠️ **「経営管理契約店舗のみ」が効く。**どの店が契約対象かで取れる範囲が変わるので、
取れなかった店を「バグ」と決めつけない。

### 3-3. 調査の結論（2026-09-20）— **原価は画面にあるが、CSVで落とせない**

経営管理タブの中身を全部開いて確認した。

```
経営管理
├ ダッシュボード /bi/flDashboard
├ 売上予測 /bi/sales-forecast ／ 日報 /bi/dailySalesReport
├ カスタムレポート /bi/customReports
├ PL           → 全店舗 /bi/monthlyPl ／ 店舗 月別 /bi/monthlyPlShop
│                 ／ 店舗 日別 /bi/dailyPl
├ POSデータ分析 → 売上速報 /bi/current-sales ／ 売上分析 /bi/sales-analytics
│                 ／ 店舗別売上一覧 /bi/company-sales-analytics
│                 ／ 曜日・時間別分析 ／ 性別・年代分析
│                 ／ 商品分析 /bi/product-analytics ／ オプション出数一覧
│                 ／ 調理・配膳時間分析 ／ 推しエール分析
├ 取引登録     → 仕入れ登録 ／ 小口現金登録 ／ 収入支出登録（**書き込み。触らない**）
└ 目標設定 ／ 科目・取引先登録 ／ 伝票指示(β)
```

**「CSVダウンロード」という項目は存在しない。** href に csv/download/export を含む
リンクも無い。サポート案内の導線は、少なくとも経営管理のサイドバーには無い。

**原価は `/bi/dailyPl`（店舗 日別PL）の画面にある。**しかも FW以外で唯一
**フードとドリンクが分かれている**。

```
粗利益 実績(累計) / 原価 / ドリンク / フード / 損益推移 / 損益計算書 / 仕入
```

**ただしこの画面に落とすボタンが無い。**押せるものを全部列挙した結果：

```
ぎふや(caret-down) / ヘルプサイト / ダイニーAI / 通知(bell)
前月(left) / 翌月(right) / すべて折りたたむ(up) / 表の項目編集 / 閉じる(down)
```

`/bi/customReports` も「新規レポート」ボタンだけ（レポートを作れば出せる可能性はあるが、
**レポート作成は書き込み**なので自動ではやらない）。

**残る可能性は3つ。**
1. カスタムレポートで原価レポートを人が1回作る → 以降は自動で落とせるかもしれない
2. サポートの言う導線は「ダイニー経営管理契約店舗」のみ。契約状況をダイニーに確認する
3. 画面から数値を読む（スクレイプ）。取り違えのリスクがあるので最後の手段

### 3-4. ⚠️ そもそもダイニーの原価が要るのか

**FWが直った今、要らない可能性が高い。**

- 24店中23店が `pos: fw`。FWのABC部門から原価率が作れる
- 1069 ひよこ飯店 / 1137 たいだい … **2026-08 から部門が取れるようになった**
- 1111 / 1151 / 1168 … 2026-07 以降は取れている
- 残る1店 1766 ぎふや福岡天神（新Uレジ）… **新Uレジ側で原価が取れた**

つまり **2026-08 以降は全店がFWか新Uレジで埋まる**。ダイニーから原価を取る必要は
無くなっている。**先に進める前に、ここを確認すること。**

### 3-3. 未確定

- 日計CSV(90列)に原価の列は無い（確認済み）
- 経営管理画面の店舗別「原価率」の読み取りは、画面がカードではなく**テーブル**だったため
  未完成。誤判定を出さないよう、全件同じ判定なら構造を出して止める作りにしてある
- 現在の日計エクスポートは**1日分しか落ちない**（`...-20260801.csv`）。
  月次には店舗別集計（開始日付/終了日付）側か、日ごとのループが要る

---

## 4. 動かし方

### 新Uレジ / ダイニー
`amami-cell/pos-sync-public` → Actions → 「POS売上取込（ダイニー/新Uレジ）」

| 入力 | 意味 |
| --- | --- |
| `month` | 対象月 `YYYY-MM`（空なら先月） |
| `diag` | `1` で診断モード。取得も書込もせず画面構造だけ出す |
| `diag_files` | `1` でスクショ/HTML/CSVも残す。**売上が写るので必要な時だけ** |

### FW
`amami-cell/hansoku` → Actions → 「FWから取り込む」
部門が取れるかの確認は `mode=abc-store-probe` / `abc_levels=部門`。

---

## 5. 重要な決めごと

- **認証情報はGitHub Actions Secretsのみ。チャットにもコードにも書かない。**
- **このリポジトリは公開。**ログに売上・原価の実数を出さない（`mask_numbers()` で `#` に伏せる）。
  画面の写しは `diag_files=1` を明示した時だけ残す
- **無料枠の中で回す。**公開リポならActionsは無料無制限。有料前提の要素を新規導入しない
- **店舗マスタは `hansoku/config/stores.yaml` が唯一の正。**pos-sync側にコピーを持たず、
  実行時に raw.githubusercontent.com から読む

---

## 6. 次の一手

1. **hansoku の `claude/abc-dept-recovered` をマージ** → 対象月のABCを再取込。
   これでひよこ飯店・たいだいの原価率が入り、不明ロスの見かけ肥大が是正される
2. **ダイニーの経営管理タブ ＞ CSVダウンロード**の入口を特定し、「原価集計（日別）」の
   列を確定する（※実原価はダイニー経営管理契約店舗のみ）
3. **新Uレジの原価CSVを月次で取る**。導線と列は確定済み（2-7）。残るのは期間指定
4. `TARGET_SHEET_ID` と `GCP_SA_JSON` をSecretsに入れる
   → 現在はローカルCSV出力。入れればシート（`POS売上`タブ）へ直接書き込む
   （年月×店舗×POS での重複排除は実装済み）
